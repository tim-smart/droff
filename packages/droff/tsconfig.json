{
  "extends": "../../tsconfig.json",
  "exclude": ["discord-api-docs"],

  "compilerOptions": {
    "outDir": "./dist"
  },

  "typedocOptions": {
    "out": "docs",
    "gitRevision": "main",
    "theme": "minimal"
  }
}
